# my-own-travel-plan

以渡假村為中心的峴港旅遊規劃網站。

## GitHub Pages

此專案使用 `.github/workflows/static.yml`，會在推送到 `main` 後部署整個 repository 根目錄。因此網站入口檔是根目錄的 `index.html`，並搭配 `styles.css` 與 `app.js`；不需要放進 `docs/`。

## 這一版可以做什麼

- 設定入住日期、渡假村、海灘區域與旅行偏好
- 由日期自動產生每天早晨／午後／夜晚的彈性行程欄位
- 收藏靈感或加入自己的願望清單
- 將規劃存於目前瀏覽器的本機儲存空間
